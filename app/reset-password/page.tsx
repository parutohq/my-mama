'use client';
import Link from 'next/link'; import { useState } from 'react'; import { createClient } from '@/lib/supabase/client';
export default function ResetPasswordPage(){const [password,setPassword]=useState('');const [message,setMessage]=useState('');const [busy,setBusy]=useState(false);async function submit(e: { preventDefault(): void }){e.preventDefault();setBusy(true);setMessage('');const {error}=await createClient().auth.updateUser({password});setMessage(error?'We could not update your password. Please request a new link.':'Your password has been updated. You can now continue to MAMA.');setBusy(false)}return <main id="main-content" className="auth-page"><section className="auth-card"><Link className="auth-brand" href="/">♥ mama.</Link><span className="auth-kicker">ACCOUNT SECURITY</span><h1>Create a new password.</h1><p>Choose a password you have not used elsewhere.</p><form onSubmit={submit}><label>New password<input required minLength={8} type="password" autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button disabled={busy}>{busy?'Updating…':'Update password'}</button></form>{message&&<output aria-live="polite">{message}</output>}<Link className="auth-link" href="/sign-in">Return to sign in</Link></section></main>}
